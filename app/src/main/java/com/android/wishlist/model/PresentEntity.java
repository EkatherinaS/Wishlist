package com.android.wishlist.model;

import androidx.room.Entity;
import androidx.room.PrimaryKey;

@Entity(tableName = "present")
public class PresentEntity {
    @PrimaryKey
    public int id;

    //TODO Finish the present entity and add other
}